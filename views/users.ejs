<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../style/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>User Creator</title>
</head>

<body>
    <div class="users-container container">
        <a class="mb-4 fw-semibold  text-decoration-none" href="/">Home <<<</a>
        <h1 class="my-3 fw-semibold">All Users</h1>

        <div class="user-data d-flex w-100 flex-wrap gap-3">
            <% if(userdata.length > 0){ %>
                <% userdata.forEach((user)=> { %>
                    <div class="user rounded p-2 d-flex flex-wrap">
                        <div class="profile-img w-100 mb-2">
                            <img src="<%=user.profileUrl%>">
                        </div>
                        <div class="profile-data w-100 d-flex gap flex-column">
                            <span class="mb-2 fw-semibold">
                                <%=user.name%>
                            </span>
                            <span class="fw-semibold" style="color: gray;">
                                <%=user.email%>
                            </span>
                        </div>
                        <div class="actions d-flex justify-content-between  align-self-end w-100 mt-2">
                            <a href="/edit/<%= user._id%>" class="text-success fw-semibold">Edit</a>
                            <a href="/delete/<%=user._id%>" class=" fw-semibold text-danger">Delete</a>
                        </div>
                    </div>
                    <%})%>
                        <%} else{%>
                            <h3 style="color: gray;" class="fw-semibold"> No User Yet</h3>
                            <% }%>


        </div>




    </div>



    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>